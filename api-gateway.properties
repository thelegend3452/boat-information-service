spring.application.name=api-gateway
server.port=8080

eureka.client.service-url.defaultZone=http://discovery-server:8761/eureka/
# Disable Spring's automatic Docker Compose
spring.docker.compose.enabled=false